services:
  app:
    build: .
    tty: true
    stdin_open: true
    command: 'tail -f /dev/null'
    volumes:
      - ./:/usr/src/app
    # depends_on:
    #   - db
    working_dir: /usr/src/app
    environment:
      DBUSER: root
      DBPASS: zzz
  # db:
  #   image: mysql:latest
  #   environment:
  #     MYSQL_ROOT_PASSWORD: zzz
  #     MYSQL_DATABASE: go_db